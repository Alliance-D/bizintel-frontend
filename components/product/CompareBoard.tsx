'use client';
import { useState } from 'react';
import { compareLocations } from '@/lib/api';
const sample:any[]=[];
export function CompareBoard(){const[result,setResult]=useState<any>(null);async function run(){setResult(await compareLocations({business_category:'salon',locations:sample}))}return <div className="space-y-6"><div className="rounded-3xl border border-white/10 bg-slate-950/70 p-6"><p className="text-xs uppercase tracking-[0.25em] text-purple-300">Compare Locations</p><h1 className="mt-3 text-3xl font-semibold text-white">Rank candidate rental spaces before you commit.</h1><button onClick={run} className="mt-5 rounded-2xl bg-purple-400 px-5 py-3 font-semibold text-slate-950">Compare locations</button></div><div className="grid gap-4 lg:grid-cols-3">{(result?.locations||[]).map((loc:any,idx:number)=><div key={`${loc.label || 'location'}-${loc.latitude || ''}-${loc.longitude || ''}-${idx}`} className="rounded-3xl border border-white/10 bg-white/[0.04] p-5"><div className="flex items-center justify-between"><h2 className="font-semibold text-white">{loc.label}</h2><span className="rounded-full bg-white/10 px-3 py-1 text-sm">#{loc.rank||idx+1}</span></div><p className="mt-4 text-4xl font-semibold text-white">{Math.round(loc.opportunity_score||0)||'—'}</p><p className="mt-2 text-sm text-slate-400">{loc.opportunity_type||'Waiting for comparison'}</p></div>)}</div>{result?.recommendation&&<div className="rounded-3xl border border-emerald-400/20 bg-emerald-400/10 p-5 text-emerald-100">{result.recommendation}</div>}</div>}

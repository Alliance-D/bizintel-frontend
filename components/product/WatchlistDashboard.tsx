'use client';
import { useEffect, useState } from 'react';
import { getAlerts, getSavedLocations } from '@/lib/api';
export function WatchlistDashboard(){const[locations,setLocations]=useState<any[]>([]);const[alerts,setAlerts]=useState<any[]>([]);useEffect(()=>{getSavedLocations().then(d=>setLocations(d.locations||[]));getAlerts().then(d=>setAlerts(d.alerts||[]));},[]);return <div className="grid gap-6 xl:grid-cols-[1fr_380px]"><main className="space-y-5"><div className="rounded-3xl border border-white/10 bg-slate-950/70 p-6"><p className="text-xs uppercase tracking-[0.25em] text-emerald-300">Watchlist</p><h1 className="mt-3 text-3xl font-semibold text-white">Return later and monitor changing opportunities.</h1></div><div className="grid gap-4 md:grid-cols-2">{locations.map(loc=><div key={loc.id} className="rounded-3xl border border-white/10 bg-white/[0.04] p-5"><h2 className="font-semibold text-white">{loc.label}</h2><p className="mt-3 text-sm text-slate-400">{loc.business_category}</p><p className="mt-5 text-3xl font-semibold text-white">{Math.round(loc.latest_opportunity_score||0)}</p><span className="text-sm text-amber-200">{loc.unread_alerts||0} alerts</span></div>)}</div></main><aside className="space-y-4"><div className="rounded-3xl border border-white/10 bg-slate-950/70 p-5"><h2 className="font-semibold text-white">Alerts</h2></div>{alerts.map(alert=><div key={alert.id} className="rounded-2xl border border-white/10 bg-white/[0.04] p-4"><p className="font-medium text-white">{alert.title}</p><p className="mt-2 text-sm leading-5 text-slate-400">{alert.message}</p></div>)}</aside></div>}

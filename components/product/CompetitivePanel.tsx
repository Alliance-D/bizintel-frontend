'use client';
import { useState } from 'react';
import { runCompetitiveAnalysis } from '@/lib/api';
import { CategorySelector } from './CategorySelector';
export function CompetitivePanel(){const[category,setCategory]=useState('salon');const[result,setResult]=useState<any>(null);async function analyze(){setResult(await runCompetitiveAnalysis({latitude:-1.9441,longitude:30.0619,business_category:category,radius_meters:1000}))}return <div className="grid gap-6 lg:grid-cols-[380px_1fr]"><aside className="rounded-3xl border border-white/10 bg-slate-950/70 p-5"><p className="text-xs uppercase tracking-[0.25em] text-cyan-300">Competitive Advantage</p><h1 className="mt-3 text-3xl font-semibold text-white">Find where you can win customers.</h1><p className="mt-3 text-sm leading-6 text-slate-400">Estimate competitor pressure, underserved pockets and strategy.</p><div className="mt-5"><CategorySelector value={category} onChange={setCategory}/></div><button onClick={analyze} className="mt-6 w-full rounded-2xl bg-cyan-400 px-5 py-3 font-semibold text-slate-950">Run analysis</button></aside><main className="grid gap-5 xl:grid-cols-3"><div className="rounded-3xl border border-white/10 bg-slate-950/60 p-6 xl:col-span-2"><h2 className="text-xl font-semibold text-white">Catchment intelligence</h2><div className="mt-6 min-h-[420px] rounded-[2rem] border border-white/10 bg-[radial-gradient(circle_at_50%_50%,rgba(34,211,238,.22),transparent_28%),linear-gradient(135deg,#020617,#0f172a)] p-6"><p className="max-w-md text-sm leading-6 text-slate-300">Phase 5 renders competitor markers, catchment zones and dominance surfaces here.</p></div></div><div className="space-y-4"><div className="rounded-3xl border border-white/10 bg-slate-950/60 p-5"><p className="font-semibold text-white">Pressure</p><p className="mt-4 text-3xl font-semibold text-white">{result?.competitor_pressure||'—'}</p><p className="mt-2 text-sm text-slate-400">{result?.competitor_count??0} competitors</p></div><div className="rounded-3xl border border-white/10 bg-slate-950/60 p-5"><p className="font-semibold text-white">Strategy</p><p className="mt-4 text-sm leading-6 text-slate-300">{result?.strategy||'Run analysis to generate strategy.'}</p></div>{result?.underserved_pockets?.map((p:any,i:number)=><div key={i} className="rounded-2xl border border-emerald-400/20 bg-emerald-400/10 p-4 text-sm text-emerald-100"><b>{p.direction}</b>: {p.reason}</div>)}</div></main></div>}

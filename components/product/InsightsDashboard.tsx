'use client';
import { useEffect, useState } from 'react';
import { getInsightsSummary } from '@/lib/api';
import { InsightCard } from './InsightCard';
export function InsightsDashboard(){const[data,setData]=useState<any>(null);useEffect(()=>{getInsightsSummary('salon').then(setData)},[]);return <div className="space-y-6"><div className="rounded-3xl border border-white/10 bg-slate-950/70 p-6"><p className="text-xs uppercase tracking-[0.25em] text-blue-300">Insights</p><h1 className="mt-3 text-3xl font-semibold text-white">Market intelligence beyond a single pin.</h1></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4">{(data?.cards||[]).map((card:any)=><InsightCard key={card.label} title={card.label} value={card.value}/>)}</div><div className="grid gap-5 lg:grid-cols-2"><div className="rounded-3xl border border-white/10 bg-white/[0.04] p-5"><h2 className="font-semibold text-white">Top opportunity cells</h2><div className="mt-4 space-y-3">{(data?.top_cells||[]).map((c:any,i:number)=><div key={c.grid_id||i} className="flex items-center justify-between rounded-2xl bg-slate-950/60 px-4 py-3"><span className="text-slate-300">{c.name||c.grid_id}</span><span className="text-emerald-200">{Math.round(c.opportunity_gap_score||0)}</span></div>)}</div></div><div className="rounded-3xl border border-white/10 bg-white/[0.04] p-5"><h2 className="font-semibold text-white">Digest</h2><div className="mt-4 space-y-3 text-sm text-slate-300">{(data?.digest||[]).map((x:string)=><p key={x}>• {x}</p>)}</div></div></div></div>}
